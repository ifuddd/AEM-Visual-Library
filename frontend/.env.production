# Production Environment
# IMPORTANT: Set this to your deployed backend URL (e.g., Railway, Render, or Vercel backend)
# For local API: use relative path (empty) or http://localhost:4000
# For production: set to your backend deployment URL
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_ENV=production
