{
  "name": "aem-visual-portal",
  "version": "1.0.0",
  "private": true,
  "description": "AEM Visual Portal - Component library with Azure DevOps Wiki integration",
  "workspaces": [
    "frontend",
    "backend",
    "sync-service",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run dev -w backend",
    "dev:sync": "npm run dev -w sync-service",
    "build": "npm run build -w shared && npm run build -w backend && npm run build -w frontend",
    "build:frontend": "npm run build -w frontend",
    "build:backend": "npm run build -w backend",
    "build:sync": "npm run build -w sync-service",
    "prisma:migrate": "npm run prisma:migrate -w backend",
    "prisma:generate": "npm run prisma:generate -w backend",
    "prisma:studio": "npm run prisma:studio -w backend",
    "test": "npm run test -w backend && npm run test -w frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
